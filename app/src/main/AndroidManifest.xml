<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.emmanuelmuturia.mymoneyapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyMoneyApp.NoActionBar">
        <activity android:name=".EditIncomeActivity"></activity>
        <activity android:name=".EditCategoryActivity" />
        <activity android:name=".EditDailyExpenseActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SplashActivity" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddCategoryActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".IncomeExpenseActivity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".IncomeActivity" />
        <activity
            android:name=".DashboardActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyMoneyApp.NoActionBar" />
    </application>

</manifest>